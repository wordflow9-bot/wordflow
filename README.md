# WordFlow — минималистичный Telegram-бот для изучения иностранных слов

WordFlow — это простой инструмент для изучения иностранных слов через тренировки внутри Telegram.
Проект использует локальное хранилище SQLite и минималистичный UX.

## Функциональность MVP
1. Работа со словарём

Добавление слова и его перевода (вручную)

Просмотр списка слов

Редактирование и удаление

Хранение всех данных в SQLite

2. Тренировки

Поддерживаются два режима:

слово → перевод

перевод → слово

Тренировочный модуль:

формирует задания

проверяет ответы

обновляет уровень освоения (mastery_level)

3. Telegram-бот

Бот предоставляет простые команды:

/add — добавить слово

/list — показать список слов

/train — начать тренировку

/help — справка

Взаимодействие построено через управляемые состояния.

## Архитектура проекта
Папка app/ — основной код приложения:

  config.py — настройки (токен бота, путь к базе данных)

  interfaces/ — абстракции (интерфейсы)

    word_repository.py — интерфейс для работы со словарём

    trainer.py — интерфейс тренировочного модуля

    bot_handlers.py — интерфейс Telegram-обработчиков

  repositories/ — слой работы с данными

    sqlite_word_repository.py — реализация WordRepository на SQLite

    database.db — файл SQLite базы проекта

  core/ — бизнес-логика приложения

    trainer_impl.py — логика тренировок

    models.py — модели данных (Word, TrainingSession и др.)

  bot/ — Telegram-обвязка

    handlers.py — обработчики команд и состояний

  main.py — точка входа, связывает все модули и запускает бота

Папка tests/ — тесты приложения:

  test_placeholder.py — заглушка для будущих тестов

Прочие файлы:

.env.example — пример переменных окружения

requirements.txt — зависимости проекта

README.md — документация

.gitignore — файлы и папки, игнорируемые Git

## Компоненты
### WordRepository (SQLite)

Файл: sqlite_word_repository.py

Реализует:

1. cоздание схемы БД

2. добавление слова

3. получение всех слов

4. обновление и удаление

5. изменение mastery_level

### Trainer

Файл: trainer_impl.py

Функции:

1. выбор слова

2. генерация вопроса

3. проверка ответа

4. изменение уровня освоения

### Telegram-бот

Файл: bot/handlers.py

Реализует:

1. команды

2. состояния

3. интеграцию с репозиторием и тренером

### Сборка проекта

Файл: main.py

Создаёт экземпляры:

1. репозитория

2. тренировочного модуля 

3. бота
  И связывает их между собой.

## Установка и запуск

1. Установите зависимости (requirements.txt)


2. Создайте .env на основе .env.example


3. Запустите бота (app.main)

4. Тестирование (pytest)

## Планы развития

1. Категории и теги слов

2. Импорт / экспорт слов 

3. Интервальные повторения

4. Улучшенная аналитика

5. Telegram mini-app (web-app)

6. Сканирование слов с бумажного словаря (распознавание текста по фото)

7. Голосовые задания (произношение → слово, слово → произношение)
